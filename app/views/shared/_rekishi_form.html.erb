<%= form_for(@rekishi) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :name, placeholder: "Compose new rekishi..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>